#!/bin/bash

# ============================================
# Songmetrix Backup Service - Cron Script
# Executa backups automatizados dentro do container
# ============================================

# Configurações
SCRIPT_DIR="/app"
LOG_FILE="/app/logs/cron.log"
BACKUP_SCRIPT="$SCRIPT_DIR/scripts/backup-orchestrator.js"
MONITORING_SCRIPT="$SCRIPT_DIR/scripts/monitoring-service.js"
CLEANUP_SCRIPT="$SCRIPT_DIR/scripts/cleanup-service.js"

# Cores para logs
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Função de log
log() {
    local level="$1"
    local message="$2"
    local timestamp=$(date '+%Y-%m-%d %H:%M:%S')
    echo -e "[$timestamp] [$level] $message" >> "$LOG_FILE"
    echo -e "[$timestamp] [$level] $message"
}

# Função para executar backup
run_backup() {
    log "INFO" "🚀 Iniciando backup completo do Songmetrix..."
    local start_time=$(date +%s)

    if node "$BACKUP_SCRIPT" >> "$LOG_FILE" 2>&1; then
        local end_time=$(date +%s)
        local duration=$((end_time - start_time))
        log "SUCCESS" "✅ Backup concluído com sucesso em ${duration}s"
        return 0
    else
        local end_time=$(date +%s)
        local duration=$((end_time - start_time))
        log "ERROR" "❌ Backup falhou após ${duration}s"
        return 1
    fi
}

# Função para executar monitoramento
run_monitoring() {
    log "INFO" "👀 Executando monitoramento..."
    if node "$MONITORING_SCRIPT" >> "$LOG_FILE" 2>&1; then
        log "SUCCESS" "✅ Monitoramento concluído"
        return 0
    else
        log "ERROR" "❌ Monitoramento falhou"
        return 1
    fi
}

# Função para executar limpeza
run_cleanup() {
    log "INFO" "🧹 Executando limpeza de backups antigos..."
    if node "$CLEANUP_SCRIPT" >> "$LOG_FILE" 2>&1; then
        log "SUCCESS" "✅ Limpeza concluída"
        return 0
    else
        log "ERROR" "❌ Limpeza falhou"
        return 1
    fi
}

# Função para backup semanal
run_weekly_backup() {
    log "INFO" "📅 Executando backup semanal..."
    # Criar backup especial semanal (pode ser mais otimizado)
    if run_backup; then
        log "SUCCESS" "✅ Backup semanal concluído"
        return 0
    else
        log "ERROR" "❌ Backup semanal falhou"
        return 1
    fi
}

# Função para backup mensal
run_monthly_backup() {
    log "INFO" "📆 Executando backup mensal..."
    # Criar backup especial mensal (compressão máxima)
    if run_backup; then
        log "SUCCESS" "✅ Backup mensal concluído"
        return 0
    else
        log "ERROR" "❌ Backup mensal falhou"
        return 1
    fi
}

# Inicialização
log "INFO" "🎯 Songmetrix Backup Service iniciado"
log "INFO" "📍 Container: $(hostname)"
log "INFO" "⏰ Timezone: $(date +%Z)"
log "INFO" "📊 Agendamento:"
log "INFO" "   • Backup diário: 02:00 AM"
log "INFO" "   • Backup semanal: Domingo 03:00 AM"
log "INFO" "   • Backup mensal: Dia 1 04:00 AM"
log "INFO" "   • Monitoramento: A cada 6 horas"

# Backup inicial (30 segundos após inicialização)
log "INFO" "⏳ Aguardando inicialização do sistema..."
sleep 30
run_backup

# Loop principal de monitoramento
log "INFO" "🔄 Iniciando loop de automação..."

while true; do
    # Obter hora atual
    HOUR=$(date +%H)
    MINUTE=$(date +%M)
    DAY=$(date +%d)
    WEEKDAY=$(date +%w)  # 0=domingo

    # Backup diário às 02:00
    if [ "$HOUR" = "02" ] && [ "$MINUTE" = "00" ]; then
        run_backup
        sleep 60  # Evitar múltiplas execuções
    fi

    # Backup semanal aos domingos às 03:00
    if [ "$WEEKDAY" = "0" ] && [ "$HOUR" = "03" ] && [ "$MINUTE" = "00" ]; then
        run_weekly_backup
        sleep 60
    fi

    # Backup mensal no dia 1 às 04:00
    if [ "$DAY" = "01" ] && [ "$HOUR" = "04" ] && [ "$MINUTE" = "00" ]; then
        run_monthly_backup
        sleep 60
    fi

    # Monitoramento a cada 6 horas (00:00, 06:00, 12:00, 18:00)
    if [ "$MINUTE" = "00" ] && { [ "$HOUR" = "00" ] || [ "$HOUR" = "06" ] || [ "$HOUR" = "12" ] || [ "$HOUR" = "18" ]; }; then
        run_monitoring
        sleep 60
    fi

    # Aguardar 1 minuto antes de verificar novamente
    sleep 60
done

# Este ponto nunca será alcançado em execução normal
log "INFO" "🛑 Songmetrix Backup Service finalizado"
exit 0